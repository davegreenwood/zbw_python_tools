import maya.cmds as cmds

sel = cmds.ls(sl=True)

target = sel[0]
object = sel[1]

pc = cmds.pointConstraint(target, object)
oc = cmds.orientConstraint(target, object)

cmds.delete(pc, oc)

cmds.select(sel[1])